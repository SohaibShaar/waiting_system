# ميزة الطباعة المتعددة للنتائج النهائية

## نظرة عامة
تم إضافة ميزة جديدة تتيح طباعة عدة سجلات من النتائج النهائية للمرضى في ملف PDF واحد بصفحات متعددة، بدلاً من الضغط على زر الطباعة لكل سجل على حدة.

## الميزات الرئيسية

### 1. اختيار السجلات
- **Checkbox في كل صف**: يمكنك تحديد أي سجل بالنقر على مربع الاختيار بجانبه
- **Checkbox في رأس الجدول**: لتحديد أو إلغاء تحديد جميع السجلات في الصفحة الحالية
- **تمييز بصري**: السجلات المحددة تظهر بخلفية زرقاء فاتحة

### 2. أزرار التحديد السريع

#### ✅ تحديد الكل
يحدد جميع السجلات الموجودة في الصفحة الحالية

#### ❌ إلغاء التحديد
يلغي تحديد جميع السجلات المحددة

#### ⚡ المستعجل فقط
يحدد فقط السجلات التي تم تصنيفها كـ "مستعجل"

#### 🔢 تحديد بالنطاق
يتيح لك تحديد السجلات حسب رقم الدور:
- **مثال 1**: من 1 إلى 10 (يحدد جميع السجلات من الدور 1 إلى الدور 10)
- **مثال 2**: من 5 إلى 20 (يحدد جميع السجلات من الدور 5 إلى الدور 20)
- **مثال 3**: من 1 إلى 4 (يحدد السجلات المستعجلة من 1 إلى 4)

### 3. زر الطباعة المتعددة
- يظهر عدد السجلات المحددة في الزر
- يطلب تأكيد قبل الطباعة
- يجمع جميع السجلات المحددة في ملف PDF واحد
- كل سجل يطبع في صفحة منفصلة
- يتم التحقق من اكتمال البيانات (فصيلة الدم) قبل الطباعة

## كيفية الاستخدام

### طريقة 1: اختيار يدوي
1. افتح قائمة الحالات المكتملة
2. حدد السجلات المطلوبة بالنقر على مربع الاختيار بجانب كل سجل
3. اضغط على زر "🖨️ طباعة"

### طريقة 2: تحديد الكل
1. افتح قائمة الحالات المكتملة
2. اضغط على "✅ تحديد الكل"
3. اضغط على زر "🖨️ طباعة"

### طريقة 3: المستعجل فقط
1. افتح قائمة الحالات المكتملة
2. اضغط على "⚡ المستعجل فقط"
3. اضغط على زر "🖨️ طباعة"

### طريقة 4: تحديد بالنطاق
1. افتح قائمة الحالات المكتملة
2. اضغط على "🔢 تحديد بالنطاق"
3. أدخل رقم البداية (مثال: 5)
4. أدخل رقم النهاية (مثال: 20)
5. اضغط على "تطبيق"
6. اضغط على زر "🖨️ طباعة"

## أمثلة عملية

### مثال 1: طباعة جميع المستعجل
```
1. افتح قائمة الحالات المكتملة
2. اضغط على "⚡ المستعجل فقط"
3. سيتم تحديد جميع السجلات المستعجلة تلقائياً
4. اضغط على "🖨️ طباعة (X) سجل"
5. أكد الطباعة
```

### مثال 2: طباعة أرقام محددة (1, 4, 5)
```
1. افتح قائمة الحالات المكتملة
2. ابحث عن الدور رقم 1 وحدده
3. ابحث عن الدور رقم 4 وحدده
4. ابحث عن الدور رقم 5 وحدده
5. اضغط على "🖨️ طباعة (3) سجل"
6. أكد الطباعة
```

### مثال 3: طباعة نطاق (من 5 إلى 20)
```
1. افتح قائمة الحالات المكتملة
2. اضغط على "🔢 تحديد بالنطاق"
3. أدخل "5" في حقل "من رقم"
4. أدخل "20" في حقل "إلى رقم"
5. اضغط على "تطبيق"
6. سيتم تحديد جميع السجلات من 5 إلى 20
7. اضغط على "🖨️ طباعة (X) سجل"
8. أكد الطباعة
```

### مثال 4: طباعة المستعجل من 1 إلى 4
```
1. افتح قائمة الحالات المكتملة
2. اضغط على "🔢 تحديد بالنطاق"
3. أدخل "1" في حقل "من رقم"
4. أدخل "4" في حقل "إلى رقم"
5. اضغط على "تطبيق"
6. الآن لديك جميع السجلات من 1 إلى 4 محددة
7. إذا كنت تريد فقط المستعجل، اضغط على "⚡ المستعجل فقط"
   (سيتم استبدال التحديد الحالي بالمستعجل فقط)
8. اضغط على "🖨️ طباعة (X) سجل"
9. أكد الطباعة
```

## ملاحظات مهمة

### ✅ المزايا
- توفير الوقت: طباعة عدة سجلات دفعة واحدة
- ملف واحد: جميع السجلات في ملف PDF واحد
- سهولة الاستخدام: واجهة بسيطة وواضحة
- مرونة: خيارات متعددة للتحديد

### ⚠️ تنبيهات
- يجب أن تكون فصيلة الدم مكتملة لجميع السجلات المحددة
- الطباعة تتم للسجلات الموجودة في الصفحة الحالية فقط
- إذا كان لديك 100 سجل موزعة على 7 صفحات، "تحديد الكل" سيحدد فقط السجلات في الصفحة الحالية
- للطباعة من صفحات متعددة، استخدم "تحديد بالنطاق"

### 💡 نصائح
- استخدم البحث لتصفية السجلات قبل التحديد
- يمكنك دمج البحث مع التحديد بالنطاق للحصول على نتائج دقيقة
- عداد السجلات المحددة يظهر في الأعلى وفي زر الطباعة
- السجلات المحددة تظهر بخلفية زرقاء فاتحة

## التقنيات المستخدمة

### Frontend (React/TypeScript)
- **State Management**: استخدام `useState` لإدارة السجلات المحددة
- **Set Data Structure**: لضمان عدم تكرار السجلات
- **Conditional Rendering**: لإظهار/إخفاء خيارات التحديد

### Printing (Canvas API)
- **Canvas**: لإنشاء صفحات PDF من البيانات
- **Promise.all**: لإنشاء جميع الصفحات بشكل متوازي
- **iframe**: لطباعة المحتوى بدون إعادة تحميل الصفحة
- **Page Break**: لفصل كل سجل في صفحة منفصلة

## الملفات المعدلة

### 1. `web/src/utils/doctorFormPrinter.js`
- إضافة دالة `printMultipleReceipts()` للطباعة المتعددة
- تصدير الدالة الجديدة مع الدالة الأصلية

### 2. `web/src/pages/DoctorPage.tsx`
- إضافة states للطباعة المتعددة
- إضافة دوال التحكم في الاختيار
- إضافة واجهة المستخدم (checkboxes وأزرار)
- إضافة دالة `handleBulkPrint()` للطباعة

## الإصدار
- **تاريخ الإضافة**: 2 نوفمبر 2025
- **الإصدار**: 1.0.0

## الدعم
للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

